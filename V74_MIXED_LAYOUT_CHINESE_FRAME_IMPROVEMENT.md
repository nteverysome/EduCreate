# v74.0 混合佈局中文框改進 - 圖片 70% 文字 30%

## 🎯 用戶需求

用戶希望中文框的佈局和英文圖片一樣：
- **有圖片 + 有文字**：圖片佔 70%，文字佔 30%（上圖下文）
- **只有文字**：只顯示文字（置中）
- **只有圖片**：只顯示圖片（置中）

## 📊 改進前後對比

### 改進前（v72.0）
```
有圖片 + 有文字：
┌─────────────────┐
│    中文圖片      │  ← 圖片在上
│                 │
├─────────────────┤
│   中文文字      │  ← 文字在下
└─────────────────┘

只有文字：
┌─────────────────┐
│   中文文字      │  ← 文字在下方
│                 │
└─────────────────┘
```

### 改進後（v74.0）
```
有圖片 + 有文字：
┌─────────────────┐
│                 │
│    中文圖片      │  ← 圖片佔 70%
│                 │
├─────────────────┤
│   中文文字      │  ← 文字佔 30%
└─────────────────┘

只有文字：
┌─────────────────┐
│                 │
│   中文文字      │  ← 文字置中
│                 │
└─────────────────┘

只有圖片：
┌─────────────────┐
│                 │
│    中文圖片      │  ← 圖片置中
│                 │
└─────────────────┘
```

## 🔧 實施的改進

**文件**：`public/games/match-up-game/scenes/game.js` 第 3016-3091 行

### 改進邏輯

現在根據內容組合分為 4 種情況：

#### 情況 1：圖片 + 文字
```javascript
// 圖片佔據上方 70%
const imageAreaHeight = cardHeightInFrame * 0.7;
const imageAreaY = -cardHeightInFrame / 2 + imageAreaHeight / 2;
const squareSize = Math.min(frameWidth - 10 - 4, imageAreaHeight - 4);

// 加載圖片
this.loadAndDisplayImage(frameContainer, pair.chineseImageUrl, 0, imageAreaY, squareSize, `chinese-${pair.id}`);

// 文字佔據下方 30%
const textAreaHeight = cardHeightInFrame * 0.3;
const textAreaY = cardHeightInFrame / 2 - bottomPadding - textHeight / 2;

// 創建文字
const chineseText = this.add.text(0, textAreaY, pair.answer, {...});
```

#### 情況 2：只有圖片
```javascript
// 圖片置中顯示
const squareSize = Math.min(frameWidth - 10 - 4, cardHeightInFrame - 4);
this.loadAndDisplayImage(frameContainer, pair.chineseImageUrl, 0, 0, squareSize, `chinese-${pair.id}`);
```

#### 情況 3：只有文字
```javascript
// 文字置中顯示
const chineseText = this.add.text(0, 0, pair.answer, {
    fontSize: `${chineseActualFontSize}px`,
    align: 'center',
    wordWrap: { width: frameWidth - 10 }
});
```

#### 情況 4：既沒有圖片也沒有文字
```javascript
// 空框（不顯示任何內容）
console.log(`⚠️ [v74.0] 混合佈局中文框 [${i}] 空框`);
```

## 📝 控制台日誌

```
🔍 [v74.0] 混合佈局中文框 [0] 內容檢查: {
  hasImage: true,
  hasText: true,
  text: "貓"
}
🖼️ [v74.0] 混合佈局中文框 [0] 圖片 + 文字模式
📝 [v74.0] 創建中文文字 [0]: "貓", 字體大小: 72px
```

## ✅ 預期結果

修復後：
- ✅ 中文框佈局和英文圖片一致
- ✅ 圖片佔 70%，文字佔 30%
- ✅ 只有文字時文字置中
- ✅ 只有圖片時圖片置中
- ✅ 用戶體驗更統一

## 🚀 提交信息

✅ **已推送到 GitHub**

**提交**：
- `14d1d0d` - fix: v74.0 改進混合佈局中文框 - 圖片70%文字30%，和英文圖片模式一致

---

**版本**：v74.0  
**狀態**：✅ 已完成並推送  
**修改文件**：`public/games/match-up-game/scenes/game.js`

