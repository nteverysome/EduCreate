# v213.0 測試指南

## 🎯 測試目標

驗證 v213.0 是否成功將卡片移動到空白框內，並且卡片可見。

## 📋 測試步驟

### 步驟 1：打開遊戲

1. 打開瀏覽器控制台（F12）
2. 訪問：http://localhost:3000/games/switcher?game=match-up-game&activityId=cmh93tjuh0001l404hszkdf94
3. 等待遊戲加載完成

### 步驟 2：點擊 "Show all answers" 按鈕

1. 在遊戲中找到 "Show all answers" 按鈕
2. 點擊按鈕
3. 觀察卡片的移動

### 步驟 3：查看控制台輸出

在控制台中搜索以下關鍵字：
- `v213.0`
- `showAllCorrectAnswers`

## 🔍 預期的控制台輸出

### 成功的輸出示例

```
🎯 [v213.0] 找到空白框: {
    pairId: 'apple',
    emptyBoxX: 500,
    emptyBoxY: 300,
    cardCurrentX: 100,
    cardCurrentY: 200,
    isInContainer: false
}

✅ [v213.0] 卡片移動完成，準備添加到容器: {
    pairId: 'apple',
    cardX: 500,
    cardY: 300
}

✅ [v213.0] 卡片已添加到容器: {
    pairId: 'apple',
    relativeX: 0,
    relativeY: 0,
    depth: 10
}
```

### 失敗的輸出示例

```
❌ [v213.0] 未找到對應的空白框: {
    pairId: 'apple',
    cardText: 'apple',
    rightEmptyBoxesCount: 4
}
```

## ✅ 驗證清單

請確認以下項目：

### 視覺驗證

- [ ] 卡片是否移動到空白框的位置？
- [ ] 卡片是否可見（沒有消失）？
- [ ] 卡片是否在空白框內（不是在框外答案卡片的位置）？
- [ ] 所有卡片是否都正確移動？

### 控制台驗證

- [ ] 是否看到 `🎯 [v213.0] 找到空白框` 的消息？
- [ ] 是否看到 `✅ [v213.0] 卡片移動完成` 的消息？
- [ ] 是否看到 `✅ [v213.0] 卡片已添加到容器` 的消息？
- [ ] `depth` 是否為 10？
- [ ] `relativeX` 和 `relativeY` 是否為 0？

### 錯誤檢查

- [ ] 是否有 `❌ [v213.0] 未找到對應的空白框` 的錯誤？
- [ ] 是否有其他錯誤消息？

## 📊 測試結果報告

請提供以下信息：

### 1. 視覺結果

描述卡片的實際行為：
- 卡片移動到哪裡？
- 卡片是否可見？
- 卡片的位置是否正確？

### 2. 控制台輸出

複製所有包含 `v213.0` 的控制台消息。

### 3. 截圖

提供以下截圖：
- 點擊 "Show all answers" 之前的遊戲畫面
- 點擊 "Show all answers" 之後的遊戲畫面
- 控制台輸出

## 🔧 可能的問題和解決方案

### 問題 1：卡片消失了

**可能原因**：
- 卡片的深度設置不正確
- 卡片被背景遮擋

**檢查**：
- 控制台中 `depth` 的值是否為 10？
- 是否有錯誤消息？

### 問題 2：卡片移動到錯誤的位置

**可能原因**：
- 找到了錯誤的空白框
- 空白框的位置不正確

**檢查**：
- 控制台中 `emptyBoxX` 和 `emptyBoxY` 的值是什麼？
- 卡片移動到的位置與這些值是否匹配？

### 問題 3：卡片沒有移動

**可能原因**：
- 沒有找到對應的空白框
- 卡片在容器中，但沒有正確移除

**檢查**：
- 是否有 `❌ 未找到對應的空白框` 的錯誤？
- 控制台中 `isInContainer` 的值是什麼？

## 🎯 v213.0 的關鍵修復

v213.0 相比 v210.0 的關鍵修復：

```javascript
// v210.0（失敗）
onComplete: () => {
    emptyBox.add(card);
    card.setPosition(0, 0);
    // ❌ 沒有設置深度
}

// v213.0（成功）
onComplete: () => {
    emptyBox.add(card);
    card.setPosition(0, 0);
    card.setDepth(10);  // ✅ 設置深度，確保卡片在背景之上
}
```

**為什麼這個修復應該有效**：
- 空白框背景的深度是 1
- 卡片的深度設置為 10
- 10 > 1，所以卡片應該在背景之上
- 卡片應該可見

## 📝 下一步

根據測試結果：

### 如果成功 ✅
- v213.0 是正確的解決方案
- 可以繼續測試其他頁面
- 可以測試換頁後的行為

### 如果失敗 ❌
- 需要分析控制台輸出
- 需要檢查卡片的實際深度
- 可能需要檢查卡片的錨點和尺寸
- 可能需要嘗試其他解決方案

## 🔍 調試技巧

如果需要更多調試信息，可以在控制台中執行：

```javascript
// 獲取遊戲場景
const scene = window.game.scene.scenes[0];

// 檢查空白框
console.log('空白框數量:', scene.rightEmptyBoxes.length);
scene.rightEmptyBoxes.forEach((box, index) => {
    console.log(`空白框 ${index}:`, {
        pairId: box.getData('pairId'),
        x: box.x,
        y: box.y,
        children: box.list.length
    });
});

// 檢查左卡片
console.log('左卡片數量:', scene.leftCards.length);
scene.leftCards.forEach((card, index) => {
    console.log(`左卡片 ${index}:`, {
        pairId: card.getData('pairId'),
        x: card.x,
        y: card.y,
        depth: card.depth,
        isInContainer: !!card.parentContainer
    });
});
```

## 📞 需要幫助？

如果測試失敗，請提供：
1. 完整的控制台輸出（包含 v213.0 的所有消息）
2. 遊戲畫面的截圖
3. 卡片的實際行為描述

這樣我就能分析問題並提供解決方案！

