# 上下容器中間空間 - 快速答案

## ❓ 您的問題

> 上下容器中間還有空間是什麼函數？

## ✅ 答案

上下容器中間的空間（紅色標示的區域）是由 **`verticalSpacing`** 變數控制的。

---

## 🎯 核心信息

### 變數名稱
```javascript
verticalSpacing  // 上下容器之間的間距
```

### 定義位置
```
文件: public/games/match-up-game/scenes/game.js
函數: createTopBottomSingleRow()
行號: L2551, L2563, L2572
```

### 計算公式
```javascript
verticalSpacing = cardHeight × verticalSpacingRatio
                = 226px × 0.01
                = 2.26px
```

### 當前值
```
verticalSpacingRatio = 0.01 (1%)
cardHeight = 226px
verticalSpacing = 2.26px
```

---

## 📍 代碼位置

### L2551 - 定義間距比例
```javascript
const verticalSpacingRatio = 0.01;  // 🔴 控制中間空間的關鍵參數
```

### L2552 - 計算理想間距
```javascript
const idealVerticalSpacing = idealCardHeight * verticalSpacingRatio;
```

### L2563 或 L2572 - 應用間距
```javascript
// 理想尺寸情況
verticalSpacing = idealVerticalSpacing;

// 或縮放情況
verticalSpacing = cardHeight * verticalSpacingRatio;
```

### L2598 - 使用間距計算下方容器位置
```javascript
const bottomY = topY + cardHeight + verticalSpacing + cardHeight / 2;
//                                   ↑
//                    這就是中間的空間！
```

---

## 🎛️ 如何調整

### 要完全移除中間空間

```javascript
// L2551
const verticalSpacingRatio = 0;  // 從 0.01 改為 0
```

**結果**: verticalSpacing = 226 × 0 = 0px

---

### 要減少中間空間 50%

```javascript
// L2551
const verticalSpacingRatio = 0.005;  // 從 0.01 改為 0.005
```

**結果**: verticalSpacing = 226 × 0.005 = 1.13px

---

### 要增加中間空間 100%

```javascript
// L2551
const verticalSpacingRatio = 0.02;  // 從 0.01 改為 0.02
```

**結果**: verticalSpacing = 226 × 0.02 = 4.52px

---

## 📊 視覺化

```
上方容器 (226px)
    ↓
┌─────────────────────┐
│  🔵 英文卡片        │
└─────────────────────┘
        ↓
    🔴 verticalSpacing (2.26px)  ← 這就是中間的空間！
        ↓
┌─────────────────────┐
│  🔴 答案卡片        │
└─────────────────────┘
下方容器 (226px)
```

---

## 🔍 驗證方法

打開瀏覽器控制台，查看日誌輸出：

```javascript
📊 [v56.0] Wordwall 風格單行布局計算: {
  ...
  verticalSpacing: 2.26,  ← 🎯 這就是中間的空間！
  verticalSpacingRatio: "1.0%"
  ...
}
```

---

## 📋 相關變數

| 變數 | 位置 | 當前值 | 說明 |
|------|------|--------|------|
| `verticalSpacingRatio` | L2551 | 0.01 | 🔴 間距比例 |
| `verticalSpacing` | L2563/2572 | 2.26px | 🔴 實際間距 |
| `cardHeight` | L2570 | 226px | 卡片高度 |
| `topY` | L2597 | 273px | 上方容器位置 |
| `bottomY` | L2598 | 603px | 下方容器位置 |

---

## 🎮 實時可視化工具

訪問以下 URL 查看實時交互式可視化：

```
http://localhost:3000/games/match-up-game/visualization/vertical-spacing-functions.html
```

**功能**:
- 🎛️ 滑塊實時調整 `verticalSpacingRatio`
- 📊 即時查看中間空間變化
- 📍 精確顯示計算公式
- 🔴 紅色區域清晰標示間距

---

## ✅ 總結

| 項目 | 答案 |
|------|------|
| **是什麼** | `verticalSpacing` 變數 |
| **在哪裡** | L2551, L2563, L2572 |
| **怎麼計算** | cardHeight × verticalSpacingRatio |
| **當前值** | 2.26px |
| **怎麼調整** | 修改 L2551 的 `verticalSpacingRatio` |
| **要移除** | 改為 0 |
| **要增加** | 改為更大的值（如 0.02） |

---

**最後更新**: 2025-01-14  
**版本**: v1.0  
**狀態**: ✅ 完整說明

