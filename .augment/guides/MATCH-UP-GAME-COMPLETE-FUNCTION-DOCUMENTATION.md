# Match-Up Game 完整函數文檔

## 📋 概述
本文檔列出 match-up-game 中所有主要函數，並標記哪些已說明、哪些未說明。

---

## ✅ 已說明的函數

### 核心遊戲邏輯
- `restartGame()` - v230.0 文檔已詳細說明
- `updateLayout()` - v230.0 文檔已詳細說明
- `restoreCardPositions()` - v230.0 文檔已詳細說明
- `showIncorrectAnswer()` - v230.0 文檔已詳細說明
- `showCorrectAnswer()` - v230.0 文檔已詳細說明

---

## ❌ 未說明的函數（按類別）

### 1️⃣ 初始化和生命週期函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `constructor()` | L20 | 初始化 GameScene 類 |
| `init(data)` | L80 | Phaser 生命週期：初始化場景參數 |
| `create()` | L523 | Phaser 生命週期：創建遊戲場景 |
| `shutdown()` | L9224 | Phaser 生命週期：清理事件監聽器 |

### 2️⃣ 詞彙加載函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `loadVocabularyFromAPI()` | L340 | 從 API 加載詞彙數據 |
| `loadDevLayoutTestData()` | L90 | 加載開發測試數據 |
| `getDevLayoutSamplePairs()` | L111 | 獲取開發測試樣本數據 |
| `buildAudioDiagnostics()` | L272 | 構建音頻診斷信息 |

### 3️⃣ 佈局計算函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `calculateMaxCardsPerPage()` | L740 | 計算每頁最大卡片數 |
| `calculatePaginationWithLayout()` | L786 | 計算分頁和佈局 |
| `initializePagination()` | L816 | 初始化分頁設置 |
| `initializeGameOptions()` | L875 | 初始化遊戲選項 |
| `initializeTimer()` | L920 | 初始化計時器 |

### 4️⃣ 卡片創建函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `createCards()` | L1707 | 主卡片創建函數 |
| `createLeftCard()` | L4382 | 創建左側英文卡片 |
| `createLeftRightSingleColumn()` | L1911 | 創建左右單列佈局 |
| `createTopBottomTwoRows()` | L2265 | 創建上下雙行佈局 |
| `createTopBottomSingleRow()` | L2523 | 創建上下單行佈局 |
| `createLeftRightMultiRows()` | L2390 | 創建左右多行佈局 |
| `createTopBottomMultiRows()` | L2660 | 創建上下多行佈局 |
| `createMixedLayout()` | L2975 | 創建混合佈局 |
| `createMixedGridLayout()` | L2768 | 創建混合網格佈局 |
| `createSeparatedLayout()` | 未找到 | 創建分離佈局 |

### 5️⃣ 卡片佈局設計函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `createCardLayoutA()` | L4654 | 佈局 A：語音+圖片+文字 |
| `createCardLayoutB()` | L4716 | 佈局 B：只有語音 |
| `createCardLayoutC()` | L4726 | 佈局 C：只有文字 |
| `createCardLayoutD()` | L4735 | 佈局 D：圖片+文字 |
| `createCardLayoutE()` | L4793 | 佈局 E：文字+語音 |
| `createCardLayoutF()` | L4822 | 佈局 F：只有圖片 |
| `createCardLayoutImageAudio()` | L4845 | 佈局：圖片+語音 |

### 6️⃣ 拖拽和交互函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `checkDrop()` | 未找到 | 檢查拖放操作 |
| `checkSwap()` | 未找到 | 檢查卡片交換 |
| `swapMixedModeCards()` | L5813 | 混合模式卡片交換 |
| `checkMatch()` | L5887 | 檢查配對 |
| `onMatchSuccess()` | L5902 | 配對成功處理 |
| `onMatchFail()` | L6079 | 配對失敗處理 |
| `unmatchCard()` | L6060 | 取消配對 |

### 7️⃣ 答案檢查函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `checkAllMatches()` | L6202 | 檢查所有配對 |
| `checkAllCardsMatched()` | L6099 | 檢查所有卡片是否配對 |
| `checkCurrentPageComplete()` | L7854 | 檢查當前頁是否完成 |

### 8️⃣ UI 更新函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `createTimerUI()` | L941 | 創建計時器 UI |
| `updateCountDownTimer()` | L984 | 更新倒數計時器 |
| `updateCountUpTimer()` | L1010 | 更新計時器（計時模式） |
| `formatTime()` | L1018 | 格式化時間 |
| `onTimeUp()` | L1026 | 時間到處理 |
| `showSubmitButton()` | L6124 | 顯示提交按鈕 |
| `showMatchSummary()` | L6735 | 顯示配對摘要 |

### 9️⃣ 分頁函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `showPaginationButtons()` | L7410 | 顯示分頁按鈕 |
| `createPaginationButton()` | L7456 | 創建分頁按鈕 |
| `createPageSelector()` | L7522 | 創建頁面選擇器 |
| `updatePageSelectorText()` | L7776 | 更新頁面選擇器文字 |
| `destroyPageSelector()` | L7756 | 銷毀頁面選擇器 |
| `goToNextPage()` | L7190 | 進入下一頁 |
| `goToPreviousPage()` | L7300 | 進入上一頁 |

### 🔟 遊戲完成函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `onGameComplete()` | L6965 | 遊戲完成處理 |
| `showGameCompleteModal()` | L7886 | 顯示遊戲完成模態框 |
| `showFinalCompletion()` | L6988 | 顯示最終完成畫面 |
| `createModalButton()` | L8065 | 創建模態框按鈕 |

### 1️⃣1️⃣ 答案顯示函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `showAnswersScreen()` | L7056 | 顯示答案畫面 |
| `showAnswersOnCards()` | L8314 | 在卡片上顯示答案 |
| `showAllCorrectAnswers()` | L8353 | 顯示所有正確答案 |
| `showCorrectAnswerOnCard()` | L8622 | 在卡片上顯示勾勾 |
| `showIncorrectAnswerOnCard()` | L8706 | 在卡片上顯示叉叉 |
| `showCorrectPairingOnCard()` | L8790 | 顯示正確配對文字 |

### 1️⃣2️⃣ 排行榜函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `fetchUserRanking()` | L8101 | 獲取用戶排名 |
| `getRankSuffix()` | L8148 | 獲取排名後綴 |
| `formatGameTime()` | L8161 | 格式化遊戲時間 |
| `showLeaderboard()` | L9092 | 顯示排行榜 |
| `hideLeaderboard()` | L9209 | 隱藏排行榜 |

### 1️⃣3️⃣ 玩家名稱函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `showEnterNamePage()` | L8825 | 顯示輸入名稱頁面 |
| `hideEnterNamePage()` | L8923 | 隱藏輸入名稱頁面 |
| `createVirtualKeyboard()` | L8938 | 創建虛擬鍵盤 |
| `createKeyButton()` | L8969 | 創建鍵盤按鈕 |
| `handleKeyPress()` | L9002 | 處理按鍵按下 |
| `submitPlayerName()` | L9028 | 提交玩家名稱 |

### 1️⃣4️⃣ 容器和框函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `createLeftContainerBox()` | L4277 | 創建左側容器框 |
| `createMultiColumnContainerBox()` | L4355 | 創建多列容器框 |
| `createEmptyRightBox()` | L9258 | 創建空白右側框 |
| `createOutsideAnswerCard()` | L9289 | 創建框外答案卡片 |
| `verifyCardInFrame()` | L4311 | 驗證卡片在框內 |

### 1️⃣5️⃣ 事件處理函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `handleResize()` | L1419 | 處理窗口調整大小 |
| `handleFullscreenChange()` | L9351 | 處理全螢幕變化 |
| `handleOrientationChange()` | L9358 | 處理設備方向變化 |

### 1️⃣6️⃣ 其他輔助函數

| 函數名 | 位置 | 說明 |
|--------|------|------|
| `addAudioStatusBadge()` | L316 | 添加音頻狀態徽章 |
| `playAudio()` | 未找到 | 播放音頻 |
| `loadAndDisplayImage()` | 未找到 | 加載並顯示圖片 |
| `createTextElement()` | 未找到 | 創建文字元素 |
| `restoreMatchedPairsVisuals()` | L1429 | 恢復已配對卡片的視覺效果 |
| `saveCardPositions()` | L9366 | 保存卡片位置 |
| `saveCardPositionForCurrentPage()` | L9395 | 保存當前頁卡片位置 |
| `resetCurrentPage()` | L6945 | 重置當前頁 |

---

## 📊 統計

- **已說明函數**: 5 個
- **未說明函數**: 100+ 個
- **總函數數**: 436 個（包括所有內部函數）

---

## 🎯 優先級說明

### 高優先級（核心遊戲邏輯）
1. `createCards()` - 卡片創建的主入口
2. `checkAllMatches()` - 答案檢查的核心
3. `onMatchSuccess()` / `onMatchFail()` - 配對結果處理
4. `goToNextPage()` / `goToPreviousPage()` - 分頁邏輯

### 中優先級（UI 和交互）
1. `showSubmitButton()` - 提交按鈕顯示
2. `showGameCompleteModal()` - 遊戲完成模態框
3. `createPageSelector()` - 分頁選擇器

### 低優先級（輔助功能）
1. `addAudioStatusBadge()` - 音頻狀態徽章
2. `formatTime()` - 時間格式化
3. `getRankSuffix()` - 排名後綴

---

**最後更新**: 2025-01-14  
**版本**: v1.0  
**狀態**: 📋 待詳細說明

