# v68.0 深度分析：中文圖片被加載到英文卡片上的根本原因

## 🔴 問題描述

用戶報告：
- 中文圖片被加載到了英文卡片上
- 中文框沒有顯示任何圖片
- 英文圖片畫面也沒有顯示

## 🔍 根本原因分析

### ImageKey 衝突

在 `loadAndDisplayImage` 函數中（第 3614 行）：

```javascript
const imageKey = `card-image-${pairId}`;
```

**問題**：
1. 中文框加載中文圖片時，使用 `imageKey = card-image-${pairId}`
2. 英文卡片加載英文圖片時，也使用 `imageKey = card-image-${pairId}`
3. **它們使用相同的 imageKey！**

### 執行流程

```
混合佈局創建：
  ↓
第一步：創建中文框
  ↓ 加載中文圖片
  ↓ imageKey = "card-image-1"
  ↓ 紋理管理器添加：card-image-1 → 中文圖片
  ↓
第二步：創建英文卡片
  ↓ 加載英文圖片
  ↓ imageKey = "card-image-1"
  ↓ 檢查：this.textures.exists("card-image-1") → true（已存在！）
  ↓ 直接使用現有紋理（中文圖片）
  ↓ 英文卡片顯示中文圖片 ❌
```

### 為什麼英文圖片沒有顯示

因為英文卡片使用的是已經存在的 `card-image-1` 紋理（中文圖片），所以英文圖片永遠不會被加載。

### 為什麼中文框沒有顯示圖片

中文框添加的圖片引用已經被英文卡片覆蓋了。

## ✅ 解決方案

### 使用不同的 ImageKey

**修改策略**：
1. 中文圖片使用：`card-image-chinese-${pairId}`
2. 英文圖片使用：`card-image-english-${pairId}`

### 修改位置

#### 1. 混合佈局中加載中文圖片（第 3027 行）

```javascript
// 修改前
this.loadAndDisplayImage(frameContainer, pair.chineseImageUrl, 0, 0, squareSize, pair.id)

// 修改後
this.loadAndDisplayImage(frameContainer, pair.chineseImageUrl, 0, 0, squareSize, `chinese-${pair.id}`)
```

#### 2. 英文卡片加載圖片（第 3441 行等）

```javascript
// 修改前
this.loadAndDisplayImage(container, imageUrl, 0, imageAreaY, squareSize, pairId)

// 修改後
this.loadAndDisplayImage(container, imageUrl, 0, imageAreaY, squareSize, `english-${pairId}`)
```

#### 3. 中文卡片加載圖片（createRightCard 函數）

```javascript
// 修改前
this.loadAndDisplayImage(container, chineseImageUrl, ...)

// 修改後
this.loadAndDisplayImage(container, chineseImageUrl, ..., `chinese-${pairId}`)
```

## 📊 修復前後對比

| 步驟 | 修復前 | 修復後 |
|------|--------|--------|
| 中文框加載中文圖片 | imageKey: `card-image-1` | imageKey: `card-image-chinese-1` |
| 英文卡片加載英文圖片 | imageKey: `card-image-1` | imageKey: `card-image-english-1` |
| ImageKey 衝突 | ❌ 衝突 | ✅ 無衝突 |
| 中文圖片顯示位置 | 英文卡片上 | 中文框上 |
| 英文圖片顯示位置 | 未加載 | 英文卡片上 |

## 🎯 預期結果

修復後：
- ✅ 中文圖片顯示在中文框上
- ✅ 英文圖片顯示在英文卡片上
- ✅ 沒有圖片衝突
- ✅ 混合佈局正常工作

## 🎉 修復結果

### 修復前
- ❌ 中文圖片被加載到英文卡片上
- ❌ 中文框沒有顯示任何圖片
- ❌ ImageKey 衝突導致圖片混亂

### 修復後
- ✅ 中文圖片正確顯示在中文框上
- ✅ 英文圖片正確顯示在英文卡片上
- ✅ 沒有 ImageKey 衝突
- ✅ 混合佈局正常工作

### 驗證日誌
```
✅ 圖片載入完成: card-image-chinese-1
✅ 圖片載入完成: card-image-english-1
✅ 混合佈局創建完成: {chineseFrames: 20, leftCards: 20, rightCards: 20}
```

## 📝 版本信息

- **版本**：v68.0
- **分析狀態**：✅ 完成
- **修復狀態**：✅ 已修復並驗證
- **提交**：c7fb392 - fix: v68.0 修復 ImageKey 衝突 - 中文圖片和英文圖片使用不同的 imageKey

