# AutoGen 編輯工作流依賴包

# 核心 AutoGen 包
autogen-agentchat[openai]>=0.4.0

# 或者安裝完整版本（包含所有擴展）
# autogen-agentchat[all]>=0.4.0

# OpenAI 客戶端（如果單獨安裝）
openai>=1.0.0

# 異步支持
asyncio-mqtt>=0.11.0

# 日誌和調試
colorlog>=6.7.0

# 可選：Jupyter 支持（用於筆記本演示）
jupyter>=1.0.0
ipywidgets>=8.0.0

# 可選：可視化工具
matplotlib>=3.5.0
graphviz>=0.20.0

# 可選：性能監控
psutil>=5.9.0

# 開發工具（可選）
black>=23.0.0
flake8>=6.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
