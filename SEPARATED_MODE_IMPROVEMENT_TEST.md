# 分離模式改進 - 測試驗證指南

## 🎯 改進內容

✅ 分離模式現在使用**自動邊距計算系統**
✅ 頂部和底部邊距**自動對稱**
✅ 邊距根據**卡片數量動態調整**
✅ 與混合模式使用**相同的計算邏輯**

---

## 📊 改進前後對比

### 改進前（v10.0）

```javascript
const leftStartY = height * 0.083;   // 固定 8.3%
const rightStartY = height * 0.083;  // 固定 8.3%
```

**問題**：
- 固定比例，不夠靈活
- 底部邊距不對稱
- 不考慮卡片數量

### 改進後（v11.0）

```javascript
const topButtonArea = 60;
const bottomButtonArea = 60;
const availableHeight = height - topButtonArea - bottomButtonArea;

const totalCardHeight = SeparatedMarginConfig.calculateTotalCardHeight(itemCount, cardHeight, leftSpacing);
const topOffset = SeparatedMarginConfig.calculateTopOffsetForSeparated(availableHeight, totalCardHeight);

const leftStartY = topButtonArea + topOffset;
const rightStartY = topButtonArea + topOffset;
const bottomOffset = topOffset;  // 自動對稱
```

**優勢**：
- 動態計算，靈活適應
- 頂部和底部邊距對稱
- 根據卡片數量調整

---

## 🧪 測試步驟

### 測試 1：3 對卡片（單列）

**預期結果**：
- 頂部邊距 ≈ 底部邊距
- 卡片完全在框內
- 計時器下方有 30px 間距

**驗證方法**：
1. 打開遊戲，選擇 3 對卡片
2. 打開 Chrome DevTools（F12）
3. 查看控制台日誌，找到：
   ```
   🎮 GameScene: 卡片位置（v11.0 改進）
   ```
4. 檢查以下值：
   - `availableHeight`：應該 ≈ 843px
   - `totalCardHeight`：應該 ≈ 450px（3 × 150）
   - `topOffset`：應該 ≈ 196px
   - `bottomOffset`：應該 ≈ 196px（與 topOffset 相同）
   - `symmetry`：應該顯示 ✅

### 測試 2：5 對卡片（單列）

**預期結果**：
- 頂部邊距 < 3 對卡片的邊距
- 卡片完全在框內
- 邊距仍然對稱

**驗證方法**：
1. 打開遊戲，選擇 5 對卡片
2. 查看控制台日誌
3. 檢查：
   - `totalCardHeight`：應該 ≈ 750px（5 × 150）
   - `topOffset`：應該 < 196px（因為內容更多）
   - `bottomOffset`：應該與 topOffset 相同

### 測試 3：10 對卡片（2 行）

**預期結果**：
- 頂部邊距更小（因為 2 行）
- 卡片完全在框內
- 邊距仍然對稱

**驗證方法**：
1. 打開遊戲，選擇 10 對卡片
2. 查看控制台日誌
3. 檢查：
   - `totalCardHeight`：應該 ≈ 320px（2 × 150 + 1 × 20）
   - `topOffset`：應該 ≈ 261px（更大，因為內容更少）
   - `bottomOffset`：應該與 topOffset 相同

### 測試 4：20 對卡片（多行）

**預期結果**：
- 頂部邊距最小（因為內容最多）
- 卡片完全在框內
- 邊距仍然對稱

**驗證方法**：
1. 打開遊戲，選擇 20 對卡片
2. 查看控制台日誌
3. 檢查：
   - `totalCardHeight`：應該 ≈ 1450px（10 × 150 + 9 × 20）
   - `topOffset`：應該最小（可能 < 10px）
   - `bottomOffset`：應該與 topOffset 相同

---

## 📐 邊距計算驗證

### 公式驗證

```
可用高度 = 屏幕高度 - 頂部按鈕 - 底部按鈕
        = 963 - 60 - 60
        = 843px

頂部偏移 = (可用高度 - 卡片總高度) / 2
        = (843 - 450) / 2
        = 196.5px

實際頂部位置 = 頂部按鈕 + 頂部偏移
            = 60 + 196.5
            = 256.5px

實際底部位置 = 實際頂部位置 + 卡片總高度 + 底部偏移
            = 256.5 + 450 + 196.5
            = 903px

驗證：903 + 60（底部按鈕）= 963px ✅
```

---

## 🔍 控制台日誌檢查

### 預期日誌格式

```javascript
🎮 GameScene: 卡片位置（v11.0 改進） {
  leftX: "672",
  rightX: "1344",
  leftStartY: "256",
  rightStartY: "256",
  containerLayout: "33%-33%-33%",
  timerGap: "30px",
  availableHeight: "843",
  totalCardHeight: "450",
  topOffset: "196",
  bottomOffset: "196",
  symmetry: "✅ 頂部和底部邊距對稱"
}
```

### 檢查清單

- [ ] `leftStartY` 和 `rightStartY` 相同
- [ ] `topOffset` 和 `bottomOffset` 相同
- [ ] `symmetry` 顯示 ✅
- [ ] `availableHeight` 正確（≈ 843px）
- [ ] `totalCardHeight` 根據卡片數量變化
- [ ] `topOffset` 根據卡片數量變化

---

## 🎨 視覺驗證

### 檢查項目

1. **頂部空間**
   - [ ] 計時器下方有 30px 間距
   - [ ] 卡片上方有充分空間
   - [ ] 空間與底部對稱

2. **卡片位置**
   - [ ] 卡片完全在框內
   - [ ] 卡片沒有被裁切
   - [ ] 卡片間距均勻

3. **底部空間**
   - [ ] 卡片下方有充分空間
   - [ ] 空間與頂部對稱
   - [ ] 沒有超出屏幕邊界

4. **左右對稱**
   - [ ] 左側卡片位置正確
   - [ ] 右側卡片位置正確
   - [ ] 左右邊距相等（150px）

---

## 🐛 常見問題排查

### 問題 1：邊距不對稱

**症狀**：`topOffset` ≠ `bottomOffset`

**解決方案**：
- 檢查 `calculateTopOffsetForSeparated()` 是否正確調用
- 驗證 `availableHeight` 計算是否正確
- 查看控制台是否有錯誤信息

### 問題 2：卡片超出框外

**症狀**：卡片被裁切或超出邊界

**解決方案**：
- 檢查 `totalCardHeight` 計算是否正確
- 驗證 `topOffset` 是否為負數
- 確認 `cardHeight` 和 `spacing` 值正確

### 問題 3：計時器間距不是 30px

**症狀**：計時器和卡片之間的間距不對

**解決方案**：
- 檢查 `topButtonArea` 是否設置為 60px
- 驗證計時器高度是否為 50px
- 計算：60 - 50 = 10px（不是 30px）
- 需要調整 `topButtonArea` 或計時器位置

---

## 📊 測試結果記錄

### 測試日期：___________

| 卡片數 | topOffset | bottomOffset | 對稱性 | 備註 |
|--------|-----------|--------------|--------|------|
| 3 | _____ | _____ | ✅/❌ | |
| 5 | _____ | _____ | ✅/❌ | |
| 10 | _____ | _____ | ✅/❌ | |
| 20 | _____ | _____ | ✅/❌ | |

---

## ✅ 測試完成檢查清單

- [ ] 所有卡片數量都測試過
- [ ] 邊距都是對稱的
- [ ] 卡片都完全在框內
- [ ] 控制台日誌正確顯示
- [ ] 視覺效果符合預期
- [ ] 沒有發現 bug

---

## 📝 改進驗證

### 改進前 vs 改進後

| 指標 | 改進前 | 改進後 |
|------|-------|-------|
| 邊距計算 | 固定比例 | 動態計算 |
| 對稱性 | ❌ 不對稱 | ✅ 完全對稱 |
| 靈活性 | ❌ 固定 | ✅ 根據內容調整 |
| 與混合模式一致 | ❌ 不一致 | ✅ 一致 |
| 用戶體驗 | 一般 | 更好 |

---

## 🎯 下一步

1. **完成所有測試**
   - 測試所有卡片數量
   - 驗證視覺效果

2. **性能測試**
   - 檢查邊距計算是否影響性能
   - 監控幀率

3. **文檔更新**
   - 更新開發文檔
   - 記錄改進內容

4. **部署**
   - 提交代碼
   - 發布更新

