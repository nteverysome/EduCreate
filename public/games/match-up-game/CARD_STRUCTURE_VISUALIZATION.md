# 英文卡片結構可視化分析

## 卡片類型：佈局 A (Layout A)

你的卡片是 **`createCardLayoutA()`** 函數創建的。

---

## 📐 卡片結構圖

```
┌─────────────────────────────────────┐
│                                     │
│  🔊 聲音按鈕                        │  ← 區域 1：20% 高度
│  (buttonAreaHeight × 45%)           │
│                                     │
├─────────────────────────────────────┤
│                                     │
│                                     │
│           📷 圖片                   │  ← 區域 2：50% 高度
│      (1:1 正方形)                   │
│                                     │
│                                     │
├─────────────────────────────────────┤
│                                     │
│           cat                       │  ← 區域 3：30% 高度
│      (自動調整字體)                 │
│                                     │
└─────────────────────────────────────┘
```

---

## 🔧 函數簽名

```javascript
createCardLayoutA(
    container,      // 卡片容器
    background,     // 背景圖片
    width,          // 卡片寬度
    height,         // 卡片高度
    text,           // 文字內容 ("cat")
    imageUrl,       // 圖片 URL
    audioUrl,       // 聲音 URL
    pairId          // 配對 ID
)
```

---

## 📍 位置計算

### 1️⃣ 聲音按鈕

```javascript
// 按鈕區域高度
const buttonAreaHeight = height * 0.2;  // 卡片高度的 20%

// 按鈕Y位置（從卡片中心計算）
const buttonAreaY = -height / 2 + buttonAreaHeight / 2;
                 = -height / 2 + height * 0.1
                 = -height * 0.4

// 按鈕大小
const buttonSize = buttonAreaHeight * 0.45;  // 按鈕區域的 45%
                = height * 0.2 * 0.45
                = height * 0.09
```

**視覺化**：
```
卡片頂部 ─────────────────────────────
         │
         │ -height/2
         │
         ├─ 🔊 按鈕 (Y = -height * 0.4)
         │
         │ -height/2 + height * 0.2
卡片中心 ─────────────────────────────
```

### 2️⃣ 圖片

```javascript
// 圖片區域高度
const imageAreaHeight = height * 0.5;  // 卡片高度的 50%

// 圖片Y位置
const imageAreaY = -height / 2 + buttonAreaHeight + imageAreaHeight / 2;
                = -height / 2 + height * 0.2 + height * 0.25
                = height * (-0.5 + 0.2 + 0.25)
                = height * (-0.05)
                = -height * 0.05

// 圖片大小（正方形）
const squareSize = Math.min(width - 4, imageAreaHeight - 4);
```

**視覺化**：
```
卡片頂部 ─────────────────────────────
         │ -height/2
         ├─ 🔊 按鈕
         │ -height/2 + height * 0.2
         ├─ 📷 圖片 (Y = -height * 0.05)
         │ -height/2 + height * 0.7
卡片中心 ─────────────────────────────
```

### 3️⃣ 文字

```javascript
// 文字區域高度
const textAreaHeight = height * 0.3;  // 卡片高度的 30%

// 底部間距
const bottomPadding = Math.max(6, height * 0.06);

// 文字高度
const textHeight = textAreaHeight - bottomPadding;

// 文字Y位置
const textAreaY = height / 2 - bottomPadding - textHeight / 2;
```

**視覺化**：
```
卡片中心 ─────────────────────────────
         │ height/2 - height * 0.3
         ├─ 文字區域開始
         │
         ├─ cat (Y = height/2 - padding - textHeight/2)
         │
         ├─ 文字區域結束
         │ height/2 - padding
卡片底部 ─────────────────────────────
```

---

## 📊 尺寸對比表

### 小卡片（20 個，高度 ≈ 50px）

| 部分 | 高度 | 計算 | 實際值 |
|------|------|------|--------|
| 按鈕區域 | 20% | 50 × 0.2 | 10px |
| 按鈕大小 | 45% | 10 × 0.45 | 4.5px |
| 圖片區域 | 50% | 50 × 0.5 | 25px |
| 文字區域 | 30% | 50 × 0.3 | 15px |

### 中卡片（10 個，高度 ≈ 75px）

| 部分 | 高度 | 計算 | 實際值 |
|------|------|------|--------|
| 按鈕區域 | 20% | 75 × 0.2 | 15px |
| 按鈕大小 | 45% | 15 × 0.45 | 6.75px |
| 圖片區域 | 50% | 75 × 0.5 | 37.5px |
| 文字區域 | 30% | 75 × 0.3 | 22.5px |

### 大卡片（7 個，高度 ≈ 100px）

| 部分 | 高度 | 計算 | 實際值 |
|------|------|------|--------|
| 按鈕區域 | 20% | 100 × 0.2 | 20px |
| 按鈕大小 | 45% | 20 × 0.45 | 9px |
| 圖片區域 | 50% | 100 × 0.5 | 50px |
| 文字區域 | 30% | 100 × 0.3 | 30px |

---

## 🔗 調用鏈

```
createCardLayoutA()
    ├─ container.add([background])
    │  └─ 添加背景層
    │
    ├─ createAudioButton()
    │  └─ 創建聲音按鈕
    │     └─ 位置：(0, buttonAreaY)
    │     └─ 大小：buttonSize
    │
    ├─ loadAndDisplayImage()
    │  └─ 加載並顯示圖片
    │     └─ 位置：(0, imageAreaY)
    │     └─ 大小：squareSize
    │
    └─ createTextElement()
       └─ 創建文字
          └─ 位置：(0, textAreaY)
          └─ 大小：自動調整
```

---

## 💾 代碼位置

**文件**：`public/games/match-up-game/scenes/game.js`

**函數**：`createCardLayoutA()` 第 5360-5420 行

**關鍵代碼片段**：

```javascript
// 1️⃣ 按鈕
const buttonAreaHeight = height * 0.2;
const buttonSize = buttonAreaHeight * 0.45;
this.createAudioButton(container, audioUrl, 0, buttonAreaY, buttonSize, pairId);

// 2️⃣ 圖片
const imageAreaHeight = height * 0.5;
const squareSize = Math.min(width - 4, imageAreaHeight - 4);
this.loadAndDisplayImage(container, imageUrl, 0, imageAreaY, squareSize, `english-${pairId}`);

// 3️⃣ 文字
const textAreaHeight = height * 0.3;
this.createTextElement(container, text, 0, textAreaY, width, textHeight);
```

---

## 🎯 特點

✅ **等比例縮放** - 所有元素都基於卡片高度的百分比
✅ **自動適應** - 卡片大小改變時，所有元素自動調整
✅ **協調佈局** - 按鈕、圖片、文字始終保持平衡
✅ **響應式設計** - 適應不同設備和卡片數量

---

**版本**：v216.0
**最後更新**：2025-11-14

