# 分離式佈局視覺分析報告

## 📐 **容器尺寸分析**

### 整體容器結構
```
┌─────────────────────────────────────────────────────────────┐
│  邊距(10-15px)                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   左側卡片   │  │  中間空白區  │  │   右側卡片   │      │
│  │   25-30%     │  │   40-50%     │  │   25-30%     │      │
│  │              │  │              │  │              │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  邊距(10-15px)                                              │
└─────────────────────────────────────────────────────────────┘
```

### 具體尺寸（基於 1920×963 容器）
| 區域 | 寬度 | 佔比 | 說明 |
|------|------|------|------|
| **左邊距** | 15px | - | 容器邊界到左側卡片 |
| **左側卡片外框** | 450-480px | 25-30% | 包含4個卡片的容器 |
| **中間空白區** | 800-900px | 40-50% | 用戶拖拽答案的區域 |
| **右側卡片區** | 450-480px | 25-30% | 答案卡片區域 |
| **右邊距** | 15px | - | 容器邊界 |

---

## 🎴 **卡片尺寸分析**

### 左側卡片（英文/圖片）
```
┌─────────────────────────────┐
│ 🔊 │ [圖片] │ air           │  ← 高度: 60-80px
│    │        │               │
└─────────────────────────────┘
  ↑    ↑        ↑              ↑
  20px 40-50px  30-40px        寬度: 150-180px
```

**組成部分：**
- **語音按鈕**：20-25px（圓形）
- **圖片區域**：40-50px 寬 × 30-40px 高
- **文字區域**：30-40px 寬
- **總寬度**：150-180px
- **總高度**：60-80px

### 右側卡片（中文/圖片）
```
┌──────────────────┐
│   [圖片]         │  ← 高度: 40-50px
│   60-80px × 40px │
├──────────────────┤
│   好             │  ← 高度: 20-30px
│   25-30px 字體   │
└──────────────────┘
  寬度: 150-180px
```

**組成部分：**
- **圖片區域**：60-80px 寬 × 40-50px 高
- **文字區域**：20-30px 高
- **總寬度**：150-180px
- **總高度**：60-80px

---

## 📏 **圖片和文字大小**

### 左側卡片內容
| 元素 | 寬度 | 高度 | 字體大小 | 說明 |
|------|------|------|---------|------|
| **語音按鈕** | 20-25px | 20-25px | - | 圓形按鈕 |
| **圖片** | 40-50px | 30-40px | - | 縮略圖 |
| **英文文字** | 30-40px | 20-25px | 16-18px | 自動調整 |

### 右側卡片內容
| 元素 | 寬度 | 高度 | 字體大小 | 說明 |
|------|------|------|---------|------|
| **圖片** | 60-80px | 40-50px | - | 較大縮略圖 |
| **中文文字** | 60-80px | 20-30px | 20-24px | 自動調整 |

---

## 🔄 **尺寸比例關係**

### 卡片內部比例
```
左側卡片 (150px × 75px):
- 語音按鈕: 20px (13%)
- 圖片: 45px (30%)
- 文字: 35px (23%)
- 邊距/間距: 50px (34%)

右側卡片 (150px × 75px):
- 圖片: 70px (47%)
- 文字: 25px (17%)
- 邊距/間距: 55px (36%)
```

### 容器內部比例
```
總寬度 (1920px):
- 左邊距: 15px (0.8%)
- 左側卡片: 450px (23.4%)
- 中間空白: 900px (46.9%)
- 右側卡片: 450px (23.4%)
- 右邊距: 105px (5.5%)

總高度 (963px):
- 上邊距: 50px (5.2%)
- 卡片區域: 800px (83%)
- 下邊距: 113px (11.8%)
```

---

## ✅ **改進建議**

### 1. 調整容器比例
```javascript
const containerConfig = {
    leftRatio: 0.25,      // 改為 25%（原 40%）
    rightRatio: 0.25,     // 改為 25%（原 40%）
    middleRatio: 0.50,    // 新增中間區域 50%
    topMargin: 0.05,      // 改為 5%（原 10%）
    bottomMargin: 0.12,   // 改為 12%（原 10%）
    sideMargin: 15,       // 保持 15px
    spacing: 10           // 保持 10px
};
```

### 2. 圖片和文字大小計算
```javascript
// 根據卡片大小自動計算
const imageSize = cardHeight * 0.5;      // 圖片高度 = 卡片高度的 50%
const textSize = cardHeight * 0.25;      // 文字高度 = 卡片高度的 25%
const fontSize = cardHeight * 0.22;      // 字體大小 = 卡片高度的 22%
```

### 3. 邊距和間距調整
```javascript
// 卡片內部邊距
const cardPadding = cardHeight * 0.1;    // 10% 的卡片高度
const elementSpacing = cardHeight * 0.08; // 8% 的卡片高度
```

---

## 📊 **當前 vs 改進對比**

| 項目 | 當前配置 | 改進後 | 改進幅度 |
|------|---------|--------|---------|
| **左側容器比例** | 40% | 25% | -37.5% ⬇️ |
| **右側容器比例** | 40% | 25% | -37.5% ⬇️ |
| **中間空白比例** | 20% | 50% | +150% ⬆️ |
| **卡片寬度** | 150px | 150px | 保持 |
| **卡片高度** | 75px | 75px | 保持 |
| **圖片大小** | 可變 | 自動計算 | 更一致 |
| **文字大小** | 可變 | 自動計算 | 更一致 |

---

## 🎯 **實施優先級**

1. ⭐⭐⭐ **高優先級**：調整容器比例（左右各 25%，中間 50%）
2. ⭐⭐⭐ **高優先級**：圖片和文字大小自動計算
3. ⭐⭐ **中優先級**：邊距和間距微調
4. ⭐ **低優先級**：響應式調整


